#!/bin/sh
# nospy-upnpd-stub
# Inert stand-in for /usr/sbin/upnpd, bind-mounted over it when lan.block
# is on. Sleeps instead of exiting so anything supervising it sees a live
# child instead of retry-storming.
sleep 2147483647
